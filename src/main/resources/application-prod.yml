spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sukko?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
lostArk:
  key: ${LOSTARK_API_KEY}

